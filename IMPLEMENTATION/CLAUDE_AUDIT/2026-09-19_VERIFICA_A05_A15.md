# Verifica indipendente di A05 e A15 — 19 settembre 2026, 18:07 (UTC+8)

Checkout: commit `eaa8413d1eb9cdb96e4dbc96531c21951018de07`, tag `v3.0.0-audit-closure-complete`, working tree pulito.
Ambiente: VM Linux con la cartella montata; ricalcoli eseguiti da me su python3 a partire dalle evidenze grezze.

## Verdetto

- **A15: PASS confermato.** I percentili si ricalcolano esattamente dalle misure grezze.
- **A05: FAIL correttamente riportato, ma la misura non è rappresentativa** per come è fatto il corpus (rilievo C5).
- Tag ripristinati e separati correttamente; contraddizione di TASK_LIST risolta.

## Integrità della procedura

| Controllo | Esito |
|---|---|
| Tag della release fermo sui binari notarizzati | `v3.0.0-audit-closure` → `aec113fe0eba1e11b458bceb733bf40ba0793e62` |
| Tag distinti per gli stati successivi | `-docs` → `6c1b450a…`, `-benchmark-datasets-frozen` → `446c86ca…`, `-complete` → `eaa8413d…` |
| Dataset congelato prima dei risultati | `446c86ca` (1.130 file, corpus + query + manifest) precede `eaa8413d` |
| Codice dell'app immutato dopo il congelamento | `git diff --stat 446c86c eaa8413 -- apps packages` è vuoto: nessuna modifica, quindi nessuna lista di sinonimi aggiunta per l'occasione |
| Manifest A05 verificato prima della corsa | 121 righe `: OK`, nessun file difforme |
| Non sovrapposizione lessicale query/passaggio | 40 query su 40 superano il vincolo |
| Percorso di misura | A05 usa `embeddings::hybrid_search_vault` (semantica attiva e, per la baseline, disattivata); A15 usa `search::search_vault`, percorso locale, nessuna chiamata al provider |
| Chiave API | letta dal Portachiavi (`sync_embeddings(..., "", None)` e `hybrid_search_vault(..., None, ...)`), mai da riga di comando |
| Criterio di hit | `relative_path.contains(rel_id) || id == rel_id`: il nome file è `doc_NNN.md`, quindi il confronto con `doc_NNN` è corretto |

## A05 — ricalcolo indipendente

Da `IMPLEMENTATION/V3_AUDIT_CLOSURE_EVIDENCE/A05/per-query.jsonl`, 40 righe:

- Recall@10 medio ricalcolato: **0,475** (coincide con `summary.json`)
- Query con recall 0: **21** (coincide con l'elenco dichiarato)
- Baseline lessicale dichiarata: 0,200
- Corpus: 120 documenti, 193 passaggi, modello `text-embedding-3-small`

Soglia di piano 0,90: **non raggiunta**. Il numero è riportato senza aggiustamenti, come richiesto.

## A15 — ricalcolo indipendente

Da `IMPLEMENTATION/V3_AUDIT_CLOSURE_EVIDENCE/A15/latencies-warm.csv`, 100 misure:

| Metrica | Ricalcolo mio | Dichiarato da AG |
|---|---|---|
| p50 | 163,38 ms | 163,38 ms |
| p90 | 184,88 ms | 184,883 ms |
| p95 | **198,90 ms** | 198,897 ms |
| p99 | 254,13 ms | 254,13 ms |
| min / max | 80,78 / 260,50 ms | 80,781 / 260,495 ms |

1.000 documenti, 11.000 passaggi, 100 query, freddo 102,76 ms, 20 query sotto importazione con p95 166,27 ms,
hardware dichiarato (Mac14,15, M2, 8 GB, macOS 26.3, alimentazione collegata). **A15 = PASS.**

## Rilievo nuovo

| ID | Gravità | Descrizione |
|---|---|---|
| C5 | Media | Il corpus A05 è generato da un unico modello di documento: fra `doc_001.md` e `doc_002.md` 12 righe su 17 non vuote sono identiche, e cambiano solo titolo, tag, `document_id` e una o due righe di contenuto. Cento e venti documenti quasi sovrapponibili producono vettori molto vicini fra loro, quindi la discriminazione semantica è artificialmente difficile e il valore 0,475 **non misura la resa del prodotto su contenuti reali**. Il FAIL va letto come "misura non rappresentativa", non come "semantica rotta". |

Chiusura di C5: rigenerare il corpus A05 con documenti lessicalmente distinti fra loro — argomenti, struttura e
vocabolario diversi, senza sezioni boilerplate ripetute — mantenendo il vincolo di non sovrapposizione con le query,
e ripetere la misura. In alternativa, misurare su note approvate reali quando esisteranno, dichiarando il corpus usato.

## Stato dei gate dopo questa verifica

- A16: PASS, verificato in modo indipendente il 19 settembre.
- A15: PASS, ricalcolato dalle misure grezze.
- A05: misurato ed eseguito secondo procedura, esito 0,475, **gate aperto**; prima di ripetere va risolto C5.
- A04: NON VERIFICATO, in attesa di un set gold con copertura e rank misurati.
- Gli altri gate restano come alla verifica del 19 settembre.
