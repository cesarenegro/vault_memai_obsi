# M10 — avanzamento verificato, 13 settembre 2026

M10 non chiusa. Il codice aggiunto è locale; il servizio Worker non è pubblicato. Nessun commit/push o nuova notarizzazione. M9 precedente conservata.

## Risultati effettivi

- Corretti i difetti riprodotti del core: lock effettivo, staging non riutilizzabile/symlink rifiutati, scritture esclusive, hash, validazione Vault e import senza overwrite. Dodici test sync-engine PASS.
- Implementato il runtime Rust e registrati i comandi IPC, Portachiavi separato, congelamento e persistenza del piano, retry limitati, ripresa e lista versioni. Test Rust: 36 lib + 18 main PASS.
- Aggiunto servizio R2 opzionale con grant limitati/scadenza, commit condizionale, verifica oggetti, idempotenza e revoca. Tre test di protocollo PASS, inclusa la lettura della catena delle release e rifiuto del vecchio commit dopo revoca.
- Banco R2 reale PASS con soli dati sintetici: upload, commit, ripetizione idempotente, byte identici al download, concorrenza con un solo vincitore, revoca. Inoltre copia privata del Vault scaricata, importata e validata READY. Report M10_EVIDENCE/r2-protocol.json. Il gateway è invocato localmente con un adapter S3 reale: non è prova del Worker pubblicato o del secondo Mac.
- pnpm test/typecheck/build PASS, controlli mirati dopo modifiche. CRM typecheck e build completa PASS; corretto un import AG che precedeva use server.
- Prova app Tauri: CREATE NEW VAULT produce READY e integrità VERIFIED; Trasferimenti risponde NOT_CONFIGURED via IPC nativo. Corretto il messaggio generico di errore individuato nel banco UI.

## Accesso e criteri residui

Serve accesso di gestione Cloudflare Workers e configurazione dei grant reali per il nuovo endpoint. Le credenziali disponibili sono quelle R2; Safari non mostrava la sessione Cloudflare richiesta ed era in uso. Richiesta all’utente pendente.

Restano collaudo nativo configurato fino al CRM autenticato, mapping degli ID reali, banco nativo di interruzione/disco pieno e revisione finale della matrice, poi nuova firma/notarizzazione/installazione. Un secondo Mac non è disponibile secondo l’utente; non certificare tale scenario attraverso due directory sullo stesso Mac.

Dettaglio operativo: TASK_LIST.md e TODO LIST.TXT. Protocollo e recupero: docs/SYNC_PROTOCOL.md e docs/SYNC_RECOVERY.md.
